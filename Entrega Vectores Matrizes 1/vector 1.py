# Inserte valores numéricos en el siguiente vector y ordénelos de forma ascendente, descendente. Muestre el menor valor y el mayor valor.

import numpy as np
datos=[]
for i in range(1,11):
    datos=int(input("Ingrese valores enteros: "))
    datos.append(datos)

for item in range:
    print (datos[i].sort())

max_value = np.min(datos)
print(max_value)

max_value = np.max(datos)
print(max_value)
